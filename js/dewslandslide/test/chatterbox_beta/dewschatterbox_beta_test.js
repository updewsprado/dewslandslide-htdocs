const { expect } = chai;

function appendInput (type, selector, name, value) {

}

describe("sendViaAlertMonitor", () => {
    it("should send SMS via alert monitor", () => {
        const trigger = sendViaAlertMonitor();
        expect();
    });
});

describe("getOldMessage", () => {
    it("should load old messages", () => {
        
    });
});

describe("getOldMessageGroup", () => {
    it("should load old message group", () => {
        
    });
});

describe("replaceKeyInputs", () => {
    it("should replace narrative to...", () => {
        
    });
});

describe("getOngoingEvents", () => {
    it("should load old message group", () => {
        
    });
});

